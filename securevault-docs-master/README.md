# SecureVault Docs

Secure, simple document workflows.

## Getting Started

1. Install dependencies:
```bash
npm install
```

2. Create `.env.local` from `.env.local.example`:
```bash
cp .env.local.example .env.local
```

3. Run the development server:
```bash
npm run dev
```

4. Open [http://localhost:3000](http://localhost:3000) in your browser.

## Project Structure

- `src/app/` - Next.js App Router pages
- `src/components/` - React components
- `public/` - Static assets and PWA files
- `scripts/` - Utility scripts

## Features

- Next.js 16 with TypeScript (strict mode)
- Tailwind CSS with dark-first theme
- PWA support (desktop + web app)
- DEV test modes for friction-free development

## Next Steps

See the canvas documentation for upcoming features:
- Canvas 1: Marketing Pricing
- Canvas 2: Auth providers
- Canvas 3: Vertical Dashboard components
- Canvas 4: Upload Page

