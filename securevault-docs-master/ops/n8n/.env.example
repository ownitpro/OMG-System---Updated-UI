# Base URL for app/api per env
SVD_WEBHOOK_BASE=https://dev.securevaultdocs.com
SVD_API_BASE=https://dev.securevaultdocs.com/api

# Shared secrets (rotate in prod)
SVD_CONTACT_SALES_SECRET=replace-me
SVD_DEMO_REQUEST_SECRET=replace-me
SVD_MARKETPLACE_SECRET=replace-me

# Email (Phase 1: send via SES or n8n SMTP creds)
SVD_SES_REGION=ca-central-1
SVD_SES_FROM="SecureVault Docs <no-reply@securevaultdocs.com>"

# Slack/Teams (optional during tests)
SVD_SLACK_WEBHOOK_URL=
SVD_TEAMS_WEBHOOK_URL=

# Storage mock (no AWS required for tests)
SVD_STORAGE_BUCKET=mock-dev
